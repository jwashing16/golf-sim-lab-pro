# Cloudflare Pages Redirects
# Ensure sitemap and robots are served correctly (not caught by SPA fallback)

/sitemap.xml /sitemap.xml 200
/robots.txt /robots.txt 200

# SPA fallback for all other routes
/* /index.html 200
